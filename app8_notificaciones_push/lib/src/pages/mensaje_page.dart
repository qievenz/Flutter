import 'package:flutter/material.dart';

class MensajePage extends StatelessWidget {
  static final route = 'mensaje';

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(route),
    );
  }
}