import 'package:flutter/material.dart';

class HomePage extends StatelessWidget {
  static final route = 'homess';

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(route),
    );
  }
}